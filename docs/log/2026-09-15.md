# 2026-09-15

## [continued] Lab 3 implementation plan completed (Tasks 19-34)
ทำไมตอนนี้: แผนเดิม (2026-09-15-lab3-toktickit.md) ถูกเขียนค้างไว้ที่ Task 18/FR-14 จาก session
ก่อนหน้าที่ token หมด — ต้องปิดให้จบก่อนเริ่ม execute จริง ไม่งั้นจะ execute จากแผนที่ไม่ครบ.

ตัดสินใจ: ใช้ /plan-pro เรียกซ้ำในไฟล์เดิม (ต่อ ไม่ใช่เขียนใหม่) ตามที่ user เลือก, รันต่อในเซสชัน
เดิม (ไม่แยก session), เก็บ output เป็น .md ตาม global override ไม่ใช่ .html ของ plan-pro default,
ใช้ spawned review 2 subagent คนละมุม (spec-coverage vs critical-correctness) แทนตรวจเอง.

ระวัง: ไฟล์แผนยาว 8171 บรรทัด/34 tasks — ก่อน execute ต้องอ่านทั้งไฟล์อีกครั้งด้วย Read เป็นช่วงๆ
(ห้าม assume จำได้จาก context เก่าที่ compact ไปแล้ว). จุดเปราะที่ reviewer เจอและแก้ไปแล้วแต่ควร
double-check ตอน execute จริง: Task 26 ต้องมี try/finally ห่อการ toggle isActive ของ
admin@toktickit.dev (ใช้ DB เดียวกับ test อื่นที่ reset แค่ครั้งเดียวต่อ run), Task 24 ใช้
STATUS_LABEL (ไม่ใช่ StatusBadge component) ใน <option>, Task 31/32 E2E ใช้ API-seeded ticket และ
timestamped email ไม่ใช่ hardcoded — ถ้า execute แล้วโค้ดจริงไม่ตรงกับที่แผนบอก ให้เชื่อโค้ด/สเปคจริง
ไม่ใช่แผน. โปรเจกต์นี้ **ไม่ใช่ git repo** (`git init` ยังไม่ได้รัน) — ต้องเช็คกับ user ก่อนถ้าจะ
เริ่ม tracked commits ตอน execute.

ยังไม่ทำ: ยังไม่ได้ execute แผนจริง (แค่วางแผนเสร็จ) — รอ user เลือก execution option
(spawn/parallel subagent/subagent-driven/inline) แล้วเริ่ม Task 1 เป็นต้นไป. ยังไม่ได้รัน
`git init` เพื่อให้ spawn_task แบบ background session ใช้ worktree ได้ (จำเป็นถ้าเลือก option 1).
